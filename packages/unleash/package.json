{
  "name": "@warungpintar/morphling-adapter-unleash",
  "version": "1.0.0",
  "description": "Unleash feature flag service adapter",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "repository": "https://gitlab.warungpintar.co/wartech/feature-toggle.git",
  "author": "wartech",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "concurrently yarn:build:*",
    "build:lib": "./scripts/build.js",
    "build:types": "tsc && tsconfig-replace-paths --project tsconfig.json",
    "dev": "./scripts/watch.js",
    "test": "concurrently yarn:test:*",
    "test:types": "tsc --noEmit"
  },
  "dependencies": {
    "@warungpintar/morphling-core": "1.0.0",
    "react-unleash-flags": "^1.3.0",
    "tiny-emitter": "^2.1.0"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "esbuild": "^0.12.8",
    "tsconfig-paths": "^3.9.0",
    "tsconfig-replace-paths": "^0.0.5",
    "typescript": "^4.3.2"
  }
}
